<nav class="navbar navbar-default navbar-fixed-top">
  <div class="content" style="margin-right:10px;">
    <%= link_to "MyRecipes", root_path, class:"btn btn-large btn-success",id: "logo" %>
    <div class="nav_item">
     <%= render 'recipes/linkRecipes' %>
    </div>
    <div class="nav_item">
      <%= link_to "Link 2", "", class:"btn btn-large btn-warning",id: "link2" %>
    </div>
    <% if logged_in? %>
      <div class="nav_item">
        <%= link_to "New recipe", new_recipe_path, class:"btn btn-large btn-warning",id: "link3" %>
      </div>
      <ul class="nav_item dropdown">
          <%= link_to '#', class: 'btn btn-default btn-info dropdown-toggle','data-toggle' => 'dropdown' do %>
            <%= current_user.name %> <b class="caret"></b>
          <% end %>
          
          <ul class="dropdown-menu">
            <li><%= link_to "View profile", chef_path(current_user) %></li>
            <li><%= link_to "Edit profile", edit_chef_path(current_user) %></li>
            <li class="divider"></li>
            <li><%= link_to "Log out", logout_path, class:"btn btn-small btn-warning" %></li>
          </ul>
      </ul>
      <% else %>
      <div class="nav_item pull-right">
        <p><%= link_to "Log in", login_path, class:"btn btn-small btn-success" %></p>
      </div>
    <% end %>
  </div>
</nav>